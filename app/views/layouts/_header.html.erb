<nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <% if user_signed_in? %>
           <a class="navbar-brand" href="/"><%= current_user.profile.name %>のブログ</a>
          <% else %>
           <a class="navbar-brand" href="/" class="btn-square-so-pop">ブログ</a>
          <% end %>
        </div>
        <div class="collapse navbar-collapse" id="navigation">
          <p class="navbar-text navbar-right"> 
          <% if user_signed_in? %>
            <%= link_to '新規投稿', new_article_path, class: 'btn btn-primary' %>
            &nbsp
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'btn btn-info' %>
          <% else %>
            <%= link_to 'ユーザー登録', new_user_registration_path, class: 'btn btn-primary' %>
            &nbsp;
            <%= link_to 'ログイン', new_user_session_path, class: 'btn btn-info' %>
          <% end %>
          </p>
        </div>
      </div>
    </nav>